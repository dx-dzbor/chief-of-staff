# /brief

Generate the morning chief-of-staff brief.

Invoke the `brief` skill (`.claude/skills/brief/SKILL.md`). Read the `me/` base and produce a
prioritized morning brief in chat. Read-only; no em-dashes.
